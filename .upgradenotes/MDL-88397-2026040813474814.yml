issueNumber: MDL-88397
notes:
  core_reportbuilder:
    - message: >-
        The base report `get_main_table()` method has been deprecated, calling
        code should instead call `get_main_table_sql()`
      type: deprecated
    - message: >-
        New method of the base report class for setting complex SQL as the main
        report table, `set_main_table_sql()`


        The `$tablealias` parameter of the existing `set_main_table()` method in
        the same class is now mandatory
      type: changed
